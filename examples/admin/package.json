{
  "name": "admin-example",
  "private": true,
  "type": "module",
  "description": "Zoijs Admin Dashboard demo (runs from source; this package.json is only for the smoke tests).",
  "scripts": {
    "dev": "npx serve -l 3520 ../..",
    "test:browser": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.61.1"
  }
}
